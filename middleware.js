module.exports = {
    read:{
    },

    // regex::
	// regex:: ... recurse match empty to * only
	    // before, during, after
	    // priority, sufficiency

    // refactor the minister keywords
    // array in pgx on finalization

    person:{
	
    },
    school:{
	
    },
    testres:{
	
    }
};
